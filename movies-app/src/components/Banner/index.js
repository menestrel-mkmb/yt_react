import styles from "./Banner.module.css"

function Banner() {
    return (
    <section className={ styles.banner__sect } >
        <span>Teste banner</span>
    </section>
    );
}

export default Banner;